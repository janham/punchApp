<% provide(:title, 'ユーザー一覧') %>

<%= will_paginate %>
<ul>
<% @users.each do |user| %>
  <li>
    <%= gravatar_for user, size: 50 %>
    <%= link_to user.name, user %>
  </li>
<% end %>
</ul>
<%= will_paginate %>